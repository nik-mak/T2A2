<!DOCTYPE html>
<html>
  <head>
    <title>Nerd Bucket</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container-fluid">
        <a class="navbar-brand" href="#">Nerd Bucket</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <%= link_to 'Home', root_path, class: 'nav-link active' %>
            </li>
            <li class="nav-item">
              <%= link_to 'Developers', users_path, class: 'nav-link'%>
            </li>
            <li class="nav-item">
              <%= link_to 'Projects', projects_path, class: 'nav-link'%>
            </li>
            <li class="nav-item">
              <%= link_to 'Skills', skills_path, class: 'nav-link'%>
            </li>
          </ul>
          <% if user_signed_in? %>
            <div class='m-1'>
              <%= link_to current_user.full_name, user_path(current_user.id) %>
            </div>
            <div class='m-1'>
              <%= button_to 'Logout', destroy_user_session_path, class: 'btn btn-outline-danger btn-sm',method: :delete, form: { data: { turbo_confirm: "Are you sure you want to logout ?" } } %>
            </div>
          <% else %>
            <div class='m-1'>
              <%= link_to 'Signup', new_user_registration_path, class: 'btn btn-outline-primary btn-sm' %>
            </div>
            <div class='m-1'>
              <%= link_to 'Login', new_user_session_path, class: 'btn btn-outline-secondary btn-sm' %>
            </div>
          <% end %>
        </div>
      </div>
    </nav>
    <div class='container'>
      <div class='row'>
        <div class='col mt-5'>
          <% if notice %>
            <p class="alert alert-info notice" role="alert"><%= notice.html_safe %></p>
          <% end %>
          <% if alert %>
            <p class="alert alert-danger" role="alert"><%= alert.html_safe %></p>
          <% end %>
          <%= yield %>
        </div>
      </div>
    </div>
  </body>
</html>
