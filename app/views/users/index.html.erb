<div class='container'>
  <div class='row'>
    <div class='col mt-5'>
      <% @users.each do |user| %>
        <p> <%= link_to "#{user.first_name} #{user.last_name}", @user %> <%= %> </p>
      <% end %>
    </div>
  </div>
</div>
