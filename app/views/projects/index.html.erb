<h1>
  Projects
  <% if user_signed_in? %>
    <%= link_to 'New Project', projects_new_path, class: "btn btn-primary btn-sm" %>
  <% end %>
</h1>
<% @projects.each do |project| %>
  <p> <%= link_to project.name, project_path(project.id) %> </p>
<% end %>
