<div class='container'>
  <div class='row'>
    <div class='col mt-5'>
      <% @projects.each do |project| %>
        <p> <%= link_to project.name, @project %> </p>
      <% end %>
    </div>
  </div>
</div>
