<h1>Skills</h1>
<div class="row">
  <% @skills.each do |skill| %>
    <div class="col-sm-3 rounded-pill border border-2 mx-5 my-2">
      <p> <%= link_to skill.name, skill_path(skill.id) %> </p>
    </div>
  <% end %>
</div>
